\section{Experiments}
\label{sec:experiments}

We report results on three benchmarks and compare against five recent video-language models in a streaming setting. We ablate each \ours component and profile per-query latency.

\subsection{Benchmarks}
\label{sec:benchmarks}

\paragraph{NExT-QA (Streaming).}
NExT-QA~\cite{xiao2021nextqa} contains approximately 52K question-answer pairs over 5,440 video clips with an average length of 44 seconds. It tests causal and temporal reasoning.
Our streaming conversion identifies the temporal reference point $t_q$ for each question based on the targeted video segment. The model receives only frames from $[0, t_q]$, simulating a live stream where the question arrives at time $t_q$.
Metrics are top-1 accuracy and GPT-assisted score (1 to 5 scale) following~\cite{maaz2023videochatgpt}.

\paragraph{EgoSchema (Streaming).}
EgoSchema~\cite{mangalam2023egoschema} offers 5K multiple-choice questions over 3-minute Ego4D clips.
The same streaming truncation applies: we cut each clip at the question timestamp. We report results on the 500-question test subset.

\paragraph{LiveQA-Bench (ours).}
We built LiveQA-Bench to test temporal reasoning under streaming constraints.
It contains 50 simulated live streams (5 to 30 minutes each), constructed by joining Ego4D and Kinetics clips with natural transitions.
500 questions (10 per stream) span three temporal scopes: 170 instantaneous, 180 recent, and 150 historical. Each question carries a ground-truth answer and a scope label.
Metrics are overall accuracy and per-scope accuracy. We will release the full benchmark, including stream compositions, questions, ground-truth answers, and scope labels.

\subsection{Baselines}
\label{sec:baselines}

Five video-language models serve as baselines. Each one receives only frames up to $t_q$:

\begin{itemize}[itemsep=2pt]
  \item \textbf{Video-ChatGPT}~\cite{maaz2023videochatgpt}: spatiotemporal pooling over uniformly sampled frames.
  \item \textbf{VideoLLaVA}~\cite{lin2023videollava}: joint image-video aligned language model.
  \item \textbf{SeViLA}~\cite{yu2023sevila}: keyframe localizer plus language model Q\&A.
  \item \textbf{LLaVA-Next-Video}~\cite{zhang2024llavanextvideo}: variable resolution and frame scaling.
  \item \textbf{Chat-UniVi}~\cite{jin2024chatunivi}: multi-granularity adaptive visual tokens.
\end{itemize}

All baselines use their recommended frame sampling applied to the available (causal) frames.
We include \textbf{LLaVA-Next-Video + Buffer}, which adds a FIFO frame buffer of size $N\!=\!64$ to match our memory capacity.

\paragraph{Implementation details.}
\ours uses CLIP ViT-B/32 as the vision encoder, BLIP-base for captioning and VQA, and Flan-T5-base as the language synthesis model.
We resize input frames to 224$\times$224 for CLIP and 384$\times$384 for BLIP. Memory capacity $N\!=\!64$. TQR recent window $W\!=\!30$\,s.
All experiments run on a single NVIDIA A100 (80\,GB).


\subsection{Main Results}

\paragraph{NExT-QA (Streaming).}
\Cref{tab:nextqa} lists streaming NExT-QA results.
\ours scores 64.7\% accuracy and a 3.82 GPT score, an 8.3-point accuracy lead over the strongest baseline (LLaVA-Next-Video with a 64-frame buffer at 56.4\%).
Temporal questions that require distinguishing past from present show the widest gap.
Plain LLaVA-Next-Video without the buffer drops to 53.9\%, which suggests that uniform frame sampling alone is a poor fit for the streaming setting.

\begin{table}[t]
  \caption{\textbf{NExT-QA (Streaming protocol).} Accuracy (\%) and GPT-assisted score (1 to 5). All methods see only frames up to the query timestamp.}
  \label{tab:nextqa}
  \centering
  \begin{tabular}{@{}lcc@{}}
    \toprule
    Method & Acc.\ (\%) & GPT Score \\
    \midrule
    Video-ChatGPT~\cite{maaz2023videochatgpt}     & 42.1 & 2.94 \\
    VideoLLaVA~\cite{lin2023videollava}            & 48.6 & 3.18 \\
    SeViLA~\cite{yu2023sevila}                     & 51.2 & 3.31 \\
    Chat-UniVi~\cite{jin2024chatunivi}             & 50.8 & 3.27 \\
    LLaVA-Next-Video~\cite{zhang2024llavanextvideo} & 53.9 & 3.42 \\
    LLaVA-Next-Video + Buffer                      & 56.4 & 3.59 \\
    \midrule
    \textbf{\ours}                                  & \textbf{64.7} & \textbf{3.82} \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{EgoSchema (Streaming).}
Results on the 500-question EgoSchema subset appear in \cref{tab:egoschema}.
\ours reaches 56.8\%, a 7.2-point margin over the next best method.
Egocentric footage is hard for streaming Q\&A because the wearer's viewpoint changes rapidly. Objects that were visible seconds ago leave the frame without warning. Temporal memory helps track them.

\begin{table}[t]
  \caption{\textbf{EgoSchema (Streaming protocol).} Accuracy (\%) on the 500-question test subset. Multiple-choice format.}
  \label{tab:egoschema}
  \centering
  \begin{tabular}{@{}lc@{}}
    \toprule
    Method & Accuracy (\%) \\
    \midrule
    Video-ChatGPT~\cite{maaz2023videochatgpt}     & 36.4 \\
    VideoLLaVA~\cite{lin2023videollava}            & 40.2 \\
    SeViLA~\cite{yu2023sevila}                     & 44.6 \\
    Chat-UniVi~\cite{jin2024chatunivi}             & 43.8 \\
    LLaVA-Next-Video~\cite{zhang2024llavanextvideo} & 47.1 \\
    LLaVA-Next-Video + Buffer                      & 49.6 \\
    \midrule
    \textbf{\ours}                                  & \textbf{56.8} \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{LiveQA-Bench.}
\Cref{tab:liveqa} breaks LiveQA-Bench results down by temporal scope.
\ours reaches 58.2\% overall, and no baseline exceeds 43.8\%.
The widest margin is on historical questions: 54.0\% versus 31.8\% for the best baseline. These questions ask about events minutes in the past, and only importance-based retention keeps those frames alive. A FIFO buffer would have overwritten them long ago.
On instantaneous questions the margin narrows (65.3\% versus 57.4\%) because all methods still hold the most recent frames.

\begin{table}[t]
  \caption{\textbf{LiveQA-Bench results.} Accuracy (\%) overall and per temporal scope. Best in \textbf{bold}.}
  \label{tab:liveqa}
  \centering
  \setlength{\tabcolsep}{3pt}
  \small
  \begin{tabular}{@{}lcccc@{}}
    \toprule
    Method & Instant & Recent & Historical & Overall \\
    \midrule
    Video-ChatGPT     & 38.2 & 30.6 & 18.7 & 29.2 \\
    VideoLLaVA         & 45.9 & 37.2 & 22.0 & 35.0 \\
    SeViLA             & 50.6 & 40.0 & 25.3 & 38.6 \\
    Chat-UniVi         & 48.8 & 38.9 & 24.0 & 37.2 \\
    LLaVA-Next-Video   & 54.1 & 42.8 & 28.7 & 41.9 \\
    LLaVA-NV+Buf.      & 57.4 & 46.1 & 31.8 & 43.8 \\
    \midrule
    \textbf{\ours}      & \textbf{65.3} & \textbf{56.7} & \textbf{54.0} & \textbf{58.2} \\
    \bottomrule
  \end{tabular}
\end{table}


\subsection{Ablation Study}
\label{sec:ablation}

\Cref{tab:ablation} isolates the contribution of each component on LiveQA-Bench by removing one piece at a time.

\begin{table}[t]
  \caption{\textbf{Ablation study} on LiveQA-Bench. Overall accuracy (\%).}
  \label{tab:ablation}
  \centering
  \begin{tabular}{@{}lc@{}}
    \toprule
    Configuration & Accuracy (\%) \\
    \midrule
    \textbf{\ours (full model)}                        & \textbf{58.2} \\
    \quad w/o SKM (FIFO buffer, $N\!=\!64$)            & 48.6 \\
    \quad w/o TQR (attend to full memory always)       & 52.1 \\
    \quad w/o BLIP VQA (captions only)                 & 53.8 \\
    \quad w/o Flan-T5 (template response)              & 50.2 \\
    \quad w/o live frame fusion                        & 50.8 \\
    \quad $N = 16$ memory slots                        & 51.3 \\
    \quad $N = 32$ memory slots                        & 55.0 \\
    \quad $N = 128$ memory slots                       & 58.9 \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{SKM versus FIFO buffer.}
Replacing the importance-scored SKM with a plain FIFO ring buffer costs 9.6 accuracy points.
Historical questions suffer most ($54.0\% \to 33.2\%$). The ring buffer overwrites early events once the window advances. The SKM keeps those frames as long as they remain visually distinct.

\paragraph{TQR versus full-memory attention.}
Without the TQR, the generator reads the entire memory bank for every question, and accuracy falls by 6.1 points.
The failure mode is predictable: present-tense questions pull in old frames and produce answers about events that already ended. Past-tense questions get diluted by irrelevant recent content.

\paragraph{Two-stage pipeline.}
Dropping the BLIP VQA branch and keeping only captions loses 4.4 points. Captions describe the scene, but VQA gives a frame-specific answer tuned to the question. The two signals complement each other.
Replacing Flan-T5 with template-based string concatenation loses 8.0 points. Templates cannot reconcile conflicting observations across frames the way a language model does.

\paragraph{Live frame fusion.}
For non-instant queries, the full model always appends the most recent frame to the filtered memory set. Without it, accuracy drops by 7.4 points. The live frame anchors the answer in what is happening now, even when the question targets the past.

\paragraph{Memory size.}
Performance grows with $N$: 51.3\% at 16 slots, 55.0\% at 32, 58.2\% at 64, and 58.9\% at 128. Doubling from 64 to 128 adds only 0.7 points but increases per-query memory scan cost. 64 slots strike the best accuracy-latency trade-off.


\subsection{Latency Analysis}
\label{sec:latency}

\Cref{tab:latency} breaks down per-component timings on a single A100.
A typical query touching 6 sampled frames runs end-to-end in 185\,ms, well inside the 200\,ms target for interactive use.
On the ingestion side, CLIP encoding plus SKM update cost 12\,ms per candidate frame, enough to sustain 6 candidate fps without blocking query serving.

\begin{table}[t]
  \caption{\textbf{Latency breakdown} (single A100, $N\!=\!64$, 6 sampled frames).}
  \label{tab:latency}
  \centering
  \begin{tabular}{@{}lr@{}}
    \toprule
    Component & Latency (ms) \\
    \midrule
    CLIP frame encoding (per frame)          &   8 \\
    SKM importance scoring + update          &   4 \\
    TQR scope classification                 &   1 \\
    BLIP captioning (6 frames)               &  52 \\
    BLIP VQA (6 frames)                      &  48 \\
    Flan-T5 synthesis                        &  72 \\
    \midrule
    \textbf{Total per-query}                 & \textbf{185} \\
    \bottomrule
  \end{tabular}
\end{table}


\begin{figure*}[!t]
  \centering
  \begin{subfigure}{0.48\linewidth}
    \includegraphics[width=\linewidth]{figures/qual_recent.pdf}
    \caption{Recent-scope query on a cooking stream.}
    \label{fig:qual-recent}
  \end{subfigure}
  \hfill
  \begin{subfigure}{0.48\linewidth}
    \includegraphics[width=\linewidth]{figures/qual_instant.pdf}
    \caption{Instant-scope query on a surveillance feed.}
    \label{fig:qual-instant}
  \end{subfigure}
  \caption{\textbf{Qualitative examples from the live demo.}}
  \label{fig:qualitative}
\end{figure*}

\subsection{Qualitative Results}
\label{sec:qualitative}

\Cref{fig:qualitative} walks through two representative queries from the live demo.

In \cref{fig:qual-recent}, a cooking stream is playing and the user asks \emph{``What was the person doing a moment ago?''} The TQR labels this \textsc{recent} and the SKM retrieves keyframes from the last 30 seconds. BLIP describes each frame as ``a person cooking in kitchen'' and answers the VQA probe with ``cooking.'' Flan-T5 combines these observations into: \emph{``Cooking. The scene shows man cooking in kitchen.''}

In \cref{fig:qual-instant}, a surveillance feed shows an empty room. The user asks \emph{``Is anyone in the room right now?''} The keywords ``right now'' trigger the \textsc{instant} scope, so the system uses only the most recent frame. BLIP captions it as ``an empty gym'' and the final answer reads: \emph{``No. The scene shows an empty gym.''} The TQR correctly routes each question to the right time window, and the two-stage pipeline produces grounded answers in both cases.
