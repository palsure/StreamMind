services:
  streammind:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MEMORY_CAPACITY=32
    volumes:
      - model-cache:/root/.cache/huggingface
    restart: unless-stopped

volumes:
  model-cache:
